# Configuración básica de la aplicación
spring.application.name=innosistemas

# Configuración de Supabase (PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.vjojyhmttbijgpnaekxg.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:InnoSistemasDB-prueba}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuraciones de seguridad con jwt
security.jwt.secret=${JWT_SECRET:MiClaveUltraSecretaQueDebeSerDeAlMenos256BitsDeLargo123456}
security.jwt.expiration=86400000
