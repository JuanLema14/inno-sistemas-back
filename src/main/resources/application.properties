# Configuración básica de la aplicación
spring.application.name=innosistemas

# Configuración de Supabase (PostgreSQL)
spring.datasource.url=jdbc:postgresql://db.vjojyhmttbijgpnaekxg.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=InnoSistemasDB-prueba
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuraciones de seguridad con jwt
security.jwt.secret=MiClaveUltraSecretaQueDebeSerDeAlMenos256BitsDeLargo123456
security.jwt.expiration=86400000
